name: Test react app
on: push
jobs:
  test:
    truns-on: ubuntu-latest
    steps:
      - name: Get the repos
        uses: actions/checkout@v6
      - name: Install depend
        run: npm ci
      - name: Test app
        run: npm run test